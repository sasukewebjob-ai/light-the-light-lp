# Claude Codeへの申し送り（2026-09-09）

**セッション終了時点の保存記録。** ユーザーから作業終了・記録保存の指示を受領。改修ファイルはローカル保存済み。次回は以下の未確認事項から再開し、ユーザーの最新指示に沿って公開作業を進める。

## 今回の着地点
ユーザーの「ライトザライトのメインはグルコン」「みんなで学ぶ・共有する・仲間になる・稼ぐを強く出したい」に合わせ、ローカルLPを改修した。プッシュはClaude Codeが担当する。

ヒーローは「みんなで学ぶ。実践を共有する。仲間と、稼ぐ力へ。」。グルコンの開催案内を前半に置き、学び合い・共有・仕事への活用をタブで説明する。アーカイブと視聴案内はグルコンの合間を支える機能とした。料金は月5,500円のコミュニティを主役にし、個別面談を追加支援として表示する。

白・深緑・ゴールドを基調にデザインを再構成。実際の活動写真を使い、外部ライブラリなしでタブ、FAQ、スクロール時の出現、現在地表示、モバイル固定CTAを実装した。CSSとJSを独立ファイルに分離している。

## 確認済み
- `python scripts/check-lp.py`：エラー0。54個のID、11個のLINE CTA。HTMLのタグ対応、ID重複、アンカー、ローカル画像/CSS/JS/法的ページの参照、ARIA参照、画像alt、LINE計測属性を検査。
- `node --check lp.js`：構文エラーなし。
- ローカルHTTPサーバーでindex.html、CSS、JS、新OGP、プロフィール・活動写真5枚、法的ページ3つはすべて200応答。
- `git diff --check`：差分の空白エラーなし（GitのLF→CRLF正規化予告のみ）。
- 特商法ページの5料金と解約25日の記載はLPと一致。
- 新OGP画像は目視確認済み。文字内容、余白、切れを確認。実画像サイズ1731×909をメタ情報に反映。

## ブラウザー検証は未完了
この環境のCUAは `Browser is not available: iab`、続く自動選択でも `No browser is available` を返したため、実画面でのPC/SP表示・クリック操作・コンソールエラーは検証できていない。過去の作業ログにあるCTA位置やページ全長は旧LPの数値であり、今回の改修には適用しない。

公開前にPC1280×720・SP390×844・幅360pxで表示を確認し、必要なら調整すること。特に主CTAの画面内表示、横あふれ、タブ切替時の高さ、FAQ開閉、固定CTAと本文の重なり、キーボード操作、モーション軽減、JavaScript無効時を確認する。

## 公開前に提供内容で確認すること
1. **おすすめ視聴案内の実体**：LPには利用できる仕様として反映した。入会者が実際に案内を確認できるよう整備する。アーカイブの分析メモは `archive-viewing-order-review.md` にあるが、最終的な視聴ガイドの提供を意味しない。
2. **募集状況**：14名／20名、残り6名は2026年7月29日時点。最新情報があれば人数と日付を一緒に更新する。
3. **掲載保留の声**：「10時間→3時間」は既存のWebライター表記と共有元の大会組合せ事例に食い違いがあるため、今回のLPでは非掲載。原稿データは正本に残してある。再掲する場合は出典を確認する。

グルコンは月2回、平日21時25分から約90分、Zoom。曜日は固定しない。個別面談は50分。グルコンと個別面談の条件を混同しないこと。

## 変更ファイル
- index.html / lp.css / lp.js
- ogp-group.png（現行OGP。旧ogp.pngは保存）
- docs/copy.md / CLAUDE.md / 作業ログ.md
- scripts/check-lp.py / docs/lp-handoff.md / docs/ogp-generation.md

既存のアーカイブ調査メモも未追跡ファイルとして残っている。git diffとgit statusを確認し、意図するファイルをコミット対象にすること。CSS・JS・OGPを含めずHTMLだけ公開すると表示が壊れる。

プレビュー：`python -m http.server 8765 --bind 127.0.0.1` → http://127.0.0.1:8765/

GA4/GTMへの計測イベント送信フックはあるが、計測IDの新規導入と本番受信確認は行っていない。コミット・プッシュ・デプロイは未実施。
